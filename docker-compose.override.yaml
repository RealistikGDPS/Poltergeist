# Development overrides, loaded automatically by Compose.
# Run without them: docker compose -f docker-compose.yaml up
services:
  app:
    environment:
      - APP_DEV_MODE=true
    volumes:
      - ./app:/app/app:ro,z
